const INITTIAL_STATE = {
  list: [],
}

const rootReducer = (state = INITTIAL_STATE, action) => {
  return state;
}

// eslint-disable-next-line import/no-anonymous-default-export
export default { rootReducer };